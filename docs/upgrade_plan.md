# 업그레이드 계획 — 구현 기준 남은 작업

**2026-09-12 작성. 제출(9/20)까지 8일.**

`docs/기획서.md`(PM 전달, CLIPQ)를 지금 돌아가는 코드와 한 줄씩 대조하고, 거기서 나온 것과
기존 감사에서 남아 있던 것을 **한 곳에 모은 문서**다. 대조 결과와 PM 에게 돌려줄 내용은
`docs/기획서_리뷰.md` 에 있다.

| 문서 | 역할 |
|---|---|
| `update_plan.md` | **지나온 기록.** 마일스톤 M0~M9, 실측, 결정 로그. 왜 이렇게 됐는지는 거기 있다 |
| **이 문서** | **앞으로 할 일.** 우선순위와 비용 판단. 제출 전 / 제출 후로 가른다 |
| `handoff.md` | 지금 상태 요약과 함정. 세션 시작 시 먼저 읽는다 |

---

## 0. 지금 되는 것 — 대조의 기준선

이 아래 "남은 것"은 전부 이 기준선 위에서의 이야기다.

| 갈래 | 상태 |
|---|---|
| 원본 등록 | 유튜브 URL(yt-dlp) 또는 로컬 파일. **OAuth 없음, 소유 확인 없음** |
| 긴 영상 | 상한 30분으로 균등 분할, 무음 지점으로 경계 스냅 |
| 전사 | faster-whisper, 단어 타임스탬프 포함 |
| 주제 분할 | LLM 이 구간과 설명을 만든다(실측 95분 → 44구간) |
| 질문 수집 | **우리 시청자 페이지에서 직접.** 로그인 없음(익명 쿠키), 레이트리밋 |
| 질문 묶기 | LLM 이 대표 문장, 임베딩이 소속(θ=0.85), SQL 이 개수 |
| 답 구간 찾기 | **LLM 이 구간 설명 전부를 보고 고른다**(최대 8개). 임베딩 top-k 는 폴백 |
| 후보 만들기 | 한 번의 호출로 N개(기본 3). 시작점 보정 + 예산(30초) 강제 |
| 판정 | 자립·답변 두 관문 + 점수. N개 병렬. **렌더 전에 텍스트로** |
| 고르기 | **크리에이터가 대사를 읽고 고른다.** 판정은 추천일 뿐 |
| 컷·렌더 | 조각 1~3개, 9:16, 번인 자막, 조합이면 브릿지 카드 |
| 시청자 화면 | 질문·좋아요, 숏폼 열, 유튜브 플레이어 제어(그 초로 이동) |
| 스튜디오 | 홈 → 영상별 페이지 → 탭 4개(질문·클립·영상 준비·기록) |
| 계측 | 이벤트 7종, 클립별 퍼널(사람 수), 비용, 프롬프트·원본 응답 보관 |
| 스택 | FastAPI · React+Vite · Postgres 17(마이그레이션 6, pgvector 없음) · Gemini · ffmpeg |
| 테스트 | 406개 |

**실데이터 현황(2026-09-12)**: 영상 1편, 질문 클러스터 4개(검토 대기 2 · 답할 구간 없음 2),
후보 3개, 클립 3개, 조각 3개, 시청자 이벤트 6건.

---

## 1. 이미 있는 네 가지 — 마무리와 노출

**기획서에 없는데 이미 도는 것들이다.** 넷 다 발표 재료이면서, 넷 다 마지막 한 걸음이 남았다.

### 1-1. 흩어진 답을 하나로 잇는 조합 클립

경쟁 도구와 **가장 눈에 띄게 다른 지점**이다. 기획서에는 언급이 없다.

**되는 것.** 조각 1~3개를 시간 순으로 잇는다. 사이에 0.4초 브릿지 카드가 들어가고
"45:26에서 이어집니다" 가 뜬다. 한 번의 인코딩으로 붙여서 코덱이 어긋날 여지가 없다.
실측(2026-09-11): 25:48 조각과 45:26 조각을 이어 **18.93초 · 1080×1920 · 48kHz 스테레오**,
브릿지 한글 정상.

**남은 것.**
- [ ] 🔴 **판정을 통과한 조합이 아직 없다.** 만든 것은 사람이 판정을 무시하고 고른 것이다.
      두 조각을 붙이면 두 번째가 지시어로 시작하기 쉬워 자립성에서 떨어진다
- [ ] 조합의 **첫 조각은 시작점 보정을 못 받는다** — 당기면 예산(30초)을 넘어 두 번째 조각이
      통째로 잘리고 그러면 조합이 아니게 된다. 지금은 그대로 두고 판정에 맡긴다
- [ ] 조각 상한이 3개라는 것이 화면에 안 보인다

### 1-2. 후보를 사람이 고르는 구조

기획서 §10 리스크 표의 *"AI가 후보 5개를 제시하고 사람이 고른다"* 가 이미 돈다.

**되는 것.** 한 번의 호출로 후보 N개를 만들고 병렬로 판정한 뒤 **대사 전문과 함께** 저장하고
멈춘다. 크리에이터가 읽고 고르면 그때 컷·렌더한다. 다시 고르는 데 **LLM 비용이 0**이다 —
범위도 대사도 판정도 이미 있다. 추천은 두 관문을 통과한 것 중 최고점이고 결정이 아니다.
보정 뒤 같아진 후보는 버린다.

**남은 것.**
- [ ] **개수를 3 → 5 로 올릴지 결정.** 판정 호출이 그만큼 는다(답변당 5회 → 7회).
      무료 등급 하루 20회 기준으로 하루에 답할 수 있는 질문이 4개 → 2.8개가 된다.
      `SHORTS_ANSWER_CANDIDATES` 로 바꾼다
- [ ] 5개로 올리면 넷째·다섯째의 `label` 은 모델이 짓는다 — 화면의 한글 표기표에 없어서
      영어 낱말이 그대로 보인다
- [ ] 후보 화면을 실사용으로 본 것이 아직 한 번뿐이다

### 1-3. "이 영상엔 없지만 저 편에서 다룹니다"

데모 시나리오 6이고, **가장 똑똑해 보이는 장면**이다.

**되는 것.** 같은 채널의 발행된 다른 영상 구간을 답하기 직전에 자동 인덱싱하고, 이 영상의
최고점보다 가까우면 그 영상을 가리킨다. 시청자 화면에 안내가 뜬다. 절대 임계값을 쓰지 않는
이유는 실측에서 유사도가 0.502~0.755 좁은 띠에 몰렸기 때문이다.

**남은 것.**
- [x] **실측 확인 2026-09-13.** 두 번째 영상("삼성 출신 면접관이 말하는 합격하는 답변", 같은 채널)을
      등록한 뒤, 시청자 API 로 "면접에서 단점을 물어보면 어떻게 답해야 하나요?" 를 남기고 집계·답하기까지
      돌렸다. 시청자 화면에 "이 영상엔 없어요 · … 편에서 다룹니다" 가 떴다. **답하기 6초, generate_content
      1회** — 구간 선택이 빈 배열을 주자 후보·판정을 부르지 않고 바로 답변 불가로 갔다.
      🔴 **데모 주의**: 판정을 후하게 해 둬서 어지간한 질문은 답이 만들어진다. 교차 안내를 보여주려면 이
      영상이 **확실히 못 답하는** 질문이어야 한다. 임베딩으로 미리 잰 후보(이 영상 → 다른 편 유사도):
      단점 질문 0.589 → 0.805 · 압박 면접 0.589 → 0.716 · 자소서 0.651 → 0.712

### 1-4. 퍼널과 비용

기획서는 이걸 Console 로 미뤄 뒀는데(MVP 제외) 이미 있다.

**되는 것.** 이벤트 7종을 쌓고 클립별 퍼널을 **사람 수**로 센다(재생 → 완주 → CTA →
원본 이동 → 원본 재생). 숫자가 적을 때 비율을 쓰지 않는다. 총비용과 단계별 토큰·지연,
그리고 **프롬프트와 원본 응답**을 남긴다.

**남은 것.**
- [x] **질문당 평균 비용** — 완료 2026-09-12. 🔴 나눗셈 하나가 아니었다. `classify` 와
      `retrieve` 가 **run 에 안 묶여 있어서** 어느 질문 때문에 일어난 호출인지 알 수 없었다.
      그것부터 붙이고, 비용을 **회차 준비와 질문 답변으로 갈랐다** — 합쳐서 질문 수로 나누면
      질문이 하나일 때 전사·분할비가 통째로 그 질문에 얹힌다.
      분모는 "답한 질문" 이 아니라 **"답하기를 돌린 질문"** 이다. 못 답한 것도 돈을 썼다.
      기준 경로(크리에이터가 자기 기준으로 뽑은 것)의 run 은 분모에서 뺀다.
      **실측(영상 1편, 95분)**: 회차 준비 260원 · 질문 답변 72원(4건) · **질문당 18원**.
      🔴 전사는 로컬 연산이라 API 비용이 0이다 — 이 숫자만 보고 "전사가 싸다" 로 읽으면 안 된다
- [ ] **사람·모델 판정 일치율** — 양쪽 다 저장되는데 계산하는 코드가 없다. tease §11 이
      "첫 품질 지표" 라고 부른 것이다
- [ ] 규칙·LLM 라우팅 일치율 — `params->>'rule' = params->>'route'` 한 줄이다
- [ ] 실제 시청자 이벤트가 6건뿐이다. 리허설에서 쌓인다

---

## 2. 기획서 대조에서 나온 것

### 2-1. 🔴 CTA 목적지 — 제품 결정 (PM)

**기획서와 구현의 목적함수가 다르다.**

| | 기획서 | 구현 |
|---|---|---|
| CTA 문구 | 지원하기 · 신청하기 · 상담 예약 | 이 구간부터 원본 보기 |
| 보내는 곳 | 채널 밖(공고·랜딩) | 유튜브 원본의 그 초 |
| 목표 | 전환 | 원본 유입 |

- [ ] **먼저 정한다.** 정하기 전에는 아래 작업의 방향이 안 잡힌다
- [ ] (외부 전환으로 간다면) 영상별 CTA 링크와 문구 필드 — **작은 작업이다**.
      클립 카드와 숏폼 끝에 붙이고, `cta_click` 이 이미 있으니 계측은 그대로 쓴다
- [ ] (둘 다 간다면) 원본 유입과 외부 전환을 퍼널에서 갈라 센다

### 2-2. 미응답 질문 리포트 (기획서 Type C)

**기획서가 부산물이자 갱신의 근거로 삼은 것**이고, 데이터는 이미 다 있다.

- [x] **완료 2026-09-12.** `answers/report.py` + `GET /api/sources/{id}/report` + 스튜디오 **리포트 탭**.
      🔴 "못 답함" 을 **셋으로 갈랐다** — 영상에 없다(UNANSWERABLE) / 아직 안 눌렀다(OPEN) /
      만들었다 물렸다(DECLINED). 뜻이 달라서 크리에이터가 할 일도 다르다: 앞의 것은 다음 회차
      큐시트고, 가운데는 버튼만 누르면 되고, 뒤의 것은 다시 만들어야 한다.
      🔴 **판정 사유를 함께 준다.** "답할 구간 없음" 딱지만으로는 영상이 정말 안 다룬 건지
      검색이 빗나간 건지 구분할 수 없다.
      🔴 **"글로 복사" 버튼**이 이 탭의 산출물이다 — 담당자가 윗선에 보내는 것은 화면이 아니라
      그 글이다. 마크다운 기호를 쓰지 않는다(메일·메신저에 그대로 붙는다)
- [ ] 카테고리 분포는 2-3 에 종속
- [ ] 🔴 기획서의 **기회손실 추정치**("예상 이탈 지원자 약 18명")는 **근거 데이터가 없다.**
      전환 표본이 0이다. 숫자를 넣으려면 추정 근거를 문서에 같이 적어야 하고, 아니면 뺀다

### 2-3. 질문 카테고리 (연봉·전형·자격·일정)

기획서 Q-Rail 의 필터이자 리포트의 분포 축이다.

- [ ] 고정 분류표를 정하고 클러스터에 붙인다. **라우팅 호출에 얹으면 호출이 안 는다** —
      이미 구간 목록을 보며 판정하는 호출이 하나 있다
- [ ] 시청자 화면 필터, 스튜디오 정렬

### 2-4. 구체성 점수 (기획서 신호 1단)

기획서가 *"바이럴 스코어의 반대편"* 이라고 부른 것. 지금 판정은 자립·답변·완성도만 본다.

- [ ] 판정에 **숫자·조건·기한·검증 가능성** 축을 더한다. 판정 프롬프트 한 항목이라 비용은 0
- [ ] 후보 카드에 표시해 고르는 근거로 쓴다

### 2-5. 체크리스트 커버리지 (기획서 신호 0단)

**질문이 한 건도 없어도 도는 바닥.** 지금은 질문이 없으면 답하기가 시작되지 않는다.

- [ ] 직군별 고정 항목표(연봉·전형·자격·근무형태·팀·기술스택·성장·지원방법)를 만들고,
      구간 설명 전수로 항목별 커버리지를 낸다. 빠진 항목이 곧 미응답 리포트가 된다
- [ ] 🔴 이게 들어가면 **콜드스타트가 풀린다** — 시청자 질문이 0건인 새 영상에서도
      리포트와 클립 후보가 나온다. 기획서 5단(교차 채널 질문 풀)의 실용적 대체물이다

### 2-6. 훅 카드 · CTA 오버레이

- [ ] 클립 앞에 질문 원문 카드(기획서는 반복 횟수까지). 브릿지 카드 렌더 코드를 재사용한다
- [ ] 하단 CTA 오버레이 — 2-1 결정 뒤

### 2-7. 이름 통일

- [x] **CLIPQ 로 통일**(2026-09-16, D14). 화면·문서·제출물 전부. 레포·패키지·CLI 만 `shorts_maker`/`sm` 로 남는다

---

### 2-8. 프롬프트 층 — 관리자 기준 · 규칙 보기 · 영상 개요 — **완료 2026-09-14**

프롬프트를 네 층으로 나눴다. 근거와 경계는 `shorts_maker/standards.py` 머리 주석이 정본이다.

| 층 | 누가 | 고칠 수 있나 | 어디서 |
|---|---|---|---|
| 1. 규칙(자립성 관문·출력 형식·번호 검증) | 코드 | **아니오 — 보기만** | 스튜디오 **프롬프트** 화면 |
| 2. 관리자 기준 | 관리자, 한 번 | 예 | 프롬프트 화면 |
| 3. 영상 개요 | 영상마다 | 예 | 영상 준비 탭 |
| 4. 이번 요청 | 매번 | — | 기준 입력 또는 시청자 질문 |

- [x] 관리자 기준 저장(마이그레이션 007, **append-only** — 고친 이력이 남는다). 1000자 상한
- [x] 🔴 **넣는 곳**: 기준 순위 · 자르기 · 후보 만들기 · 판정 **점수**. 판정의 자립·답변 두 관문에는 넣지
      않는다 — 섞으면 선호가 숨은 탈락 조건이 되고 사람·모델 일치율이 뜻을 잃는다
- [x] 🔴 **안 넣는 곳**: 구간 분할(중립 자산) · 답 구간 찾기(사실 판단). `tests/test_standards.py` 가
      두 함수의 시그니처에 `standard` 가 생기면 실패한다 — 몇 달 뒤 무심코 넣는 것을 막는다
- [x] 답하기는 기준을 **run 시작에 한 번** 읽어 후보와 판정에 같은 것을 넘긴다
- [x] 1층 보기(`GET /api/prompts`): 템플릿 **원문 그대로**, 칸은 `string.Formatter` 로 원문에서 뽑고,
      "기준 적용" 표시도 원문에 `standard_block` 칸이 있는지로 판단한다. 새 칸이 생기면 층을 정하지
      않은 한 테스트가 막는다
- [x] 영상 개요 편집(`PATCH /api/sources/{id}`, 500자). 🔴 이미 나눈 구간에는 반영되지 않는다
- [x] 기준이 비어 있으면 모든 프롬프트가 이전과 **글자까지 같다**(테스트로 고정)

**남은 것.**
- [ ] 2-4 구체성 점수는 **이 기준 칸이 자연스러운 자리**다. 코드 없이 기준 문장으로 먼저 시험해 보고,
      판정 결과에 드러나지 않으면 그때 판정 스키마에 축을 더한다
- [ ] 🔴 기준은 약한 신호다. "경쟁사 이름은 절대 내보내지 않는다" 같은 **보장**이 필요해지면 코드 필터로 간다

---

### 2-9. 질문을 남기는 순간 발행 숏폼 추천 — **2026-09-15**

이미 답이 나가 있는데 시청자가 그걸 모르고 같은 걸 또 묻는 순간을 잡는다. 결정 근거는 update_plan **D13**.

**비교 방식 실측**(질문 8 · 발행 숏폼 2, 질문=RETRIEVAL_QUERY, 대상=RETRIEVAL_DOCUMENT):

| 비교 대상 | 맞는 연결 최저 | 붙으면 안 되는 것 최고 | 틈 |
|---|---|---|---|
| 클립 제목 | 0.666 | 0.745 | -0.079 |
| 구간 설명 | 0.707 | 0.744 | -0.038 |
| **실제 대사** | **0.735** | **0.720** | **+0.015** |

묶기용 벡터(SEMANTIC_SIMILARITY)로 제목과 비교하면 "쏘카 복지는 어떤가요?" 가 기술문화 숏폼에 0.870 으로
붙어 θ 0.85 를 통과했다 — 묶기와 이 기능은 같은 도구로 풀 수 없다. 셋 다 답이 있을 때는 맞는 숏폼을
골랐고, 어려운 것은 "답이 없다" 를 가르는 선이다.

- [x] 발행할 때 클립 **실제 대사**를 임베딩해 둔다(크리에이터 경로). 실패해도 발행은 되돌리지 않는다
- [x] 질문 등록: **먼저 커밋** → 비교할 발행 숏폼이 없으면 호출 0회 → 공개 경로 분당 상한 → 재시도 없는
      임베딩 1회(제한 시간) → 이 영상의 발행 숏폼하고만 비교 → 기준선 넘는 것 최대 2개
- [x] "답입니다" 가 아니라 **"비슷한 궁금증에 답한 숏폼이 있어요"** 로 보여준다. 점수는 내보내지 않는다
- [x] 질문을 묶음에 자동으로 붙이지 않는다 — 수요 집계는 [집계] 의 몫이다
- [x] 이미 발행된 숏폼 채우기: `sm answers index-clips [source_id]`
- [ ] 🔴 **기준선 0.73 은 표본 8개로 정했다.** 평가 세트(§3)에 "이 질문엔 이 숏폼" 기대값을 넣어 다시 정한다.
      `stage_calls.params` 에 질문마다 최고점과 고른 숏폼이 남으므로 실사용 데이터로도 볼 수 있다

---

### 2-10. 보기 전용 역할 · 첫 화면 — **2026-09-16**

해커톤 심사처럼 여럿이 스튜디오를 들여다보는 동안 누가 [답하기]를 누르면 Gemini 하루 할당량이 타고
실데이터가 바뀐다. 구경은 열되 행동만 막는 두 번째 비밀번호를 뒀다.

- [x] `SHORTS_READONLY_PASSWORD`. 역할이 **서명 안에** 들어가 쿠키를 고쳐 올릴 수 없다
- [x] 🔴 경계는 **HTTP 메서드**로 가른다(`deps.SAFE_METHODS`) — 엔드포인트마다 적으면 새로 생긴 것을
      빠뜨린다. 라우트 테이블을 순회하는 테스트가 행동 라우트 전부에서 403 을 확인한다
- [x] GET 인데 행동인 구간 미리보기는 그 자리에서 따로 막는다(없으면 ffmpeg 를 돌린다)
- [x] 화면: 보기 전용 배너 · 실행 함수 한 곳에서 차단 · 403 을 한 곳에서 받아 안내
- [x] `/` 첫 화면(두 문), 스튜디오는 `/studio/**` 로 이동(D12)
- [ ] 🔴 `.env` 에 `SHORTS_READONLY_PASSWORD` 를 넣어야 켜진다. 안 넣으면 관리자 비밀번호 하나로 동작한다
- [ ] 두 비밀번호가 같으면 **기동을 거부**한다 — 배포 때 다른 값인지 확인한다

---

### 2-11. 여럿이 동시에 들어올 때 — **2026-09-16**

보기 전용 비밀번호를 공개하면서 "혼자 쓰는 도구" 전제가 깨졌다. 실측이 아니라 코드를 읽어 무엇이
깨지는지부터 확인했다: 시청자·보기 전용은 잡을 못 띄우므로 CPU·메모리 위험이 없고, 커넥션 풀 8개면
ms 단위 조회 수십 배를 받아넘긴다. 실제로 터지는 자리는 둘이었다.

- [x] **로그인 잠금이 전역이었다.** 남 다섯 명의 오타(복사에 딸린 공백 하나면 충분)가 관리자까지
      60초 잠갔다 → IP 별 + 전역 백스톱 + 공백 허용 + 창(60초) 기준 집계
- [x] **실행이 무한히 쌓였다.** 워커가 1개라 안전하긴 했지만 다섯 명이 누른 다섯 개가 조용히 줄을
      서고 할당량이 탔다 → `jobs.Busy` → **409**, 무엇이 몇 초째 도는지 함께 알려준다
- [x] 화면에 "다른 창에서 … 실행 중" 배너(5초 주기). 🔴 누가 눌렀는지는 못 쓴다 — 관리자 비밀번호가
      하나라 사람을 구분하지 않는다
- [x] 커넥션 풀 크기를 `SHORTS_DB_POOL_MAX` 로 뺐다(기본 8). 🔴 올리려면 db 컨테이너 `mem_limit`
      도 같이 올린다 — Postgres 는 연결마다 프로세스를 띄운다
- [x] 풀 20 + db `mem_limit` 600m 으로 결정(2026-09-16). **실측**: 연결 하나 1.8MB, 20연결 유지 시
      db 컨테이너 47MiB → 81MiB(상한 400MiB). 즉 연결은 문제가 아니었고, 상한을 올린 이유는
      autovacuum(64MB × 워커 3)과 work_mem(4MB × 정렬 × 연결)의 꼬리다. VM 배분은
      shorts 2600m + db 600m + OS ~690MB 가 됐다

---

## 3. 감사에서 남은 것 (기획서와 무관)

2026-09-09 감사 목록의 잔여분이다. 자세한 근거는 `handoff.md` §7.

- [ ] 🔴 **답변 경로 평가 세트** — `eval/questions.json` 에 `type`·`expect` 가 없고
      `sm tease eval-answer` 가 없다. 라우팅·후보·조합·답변 불가가 질문 세트로 측정된 적이 없다.
      **본선 발표의 품질 근거가 여기서 나온다**
- [x] 🔴 **M9 리허설 도구** — 완료 2026-09-12. `sm tease check | seed | unseed | reset`.
      🔴 **pg_dump 스냅샷을 쓰지 않는다.** 앱 이미지에 없기도 하지만, 그보다 **되돌리고 싶은 것이
      DB 전체가 아니기 때문**이다. 전사·구간 분할·임베딩은 편당 수십 분이라 리허설마다 날리면
      리허설 자체를 못 한다. 그래서 표적 초기화다.
      - `check` — 데모 8단계의 전제를 한 번에 본다. 🔴 막힌 게 있어도 종료 코드는 0이다.
        점검 도구가 실패한 게 아니라 결과를 보고한 것이다
      - `seed` — 질문을 좋아요와 함께 넣는다. **[집계]는 하지 않는다** — 그 버튼을 누르는 것이
        데모 2단계다. 좋아요를 넣는 이유는 수요 순 정렬이 화면에 보여야 하기 때문이다
      - `unseed` — **시드한 것만** 지운다(`viewer_id` 표식). 진짜 시청자 질문과 섞이면 수요
        순위가 거짓이 되는데, 그게 이 제품이 보여주려는 바로 그 숫자다
      - `reset` — 회차를 되돌린다. 🔴 `stage_calls` 를 **run 보다 먼저** 지운다 —
        `run_id` 가 `on delete set null` 이라 순서를 거꾸로 하면 답변 비용이 **회차 준비 비용으로
        둔갑하고**, 리허설을 돌릴수록 준비비가 불어나는 거짓 숫자가 만들어진다
- [ ] 🔴 **리허설 3회** — 도구는 생겼고 아직 한 번도 안 돌렸다. 시나리오 1~8 을 시간 재며.
      Gemini 장애 시 시나리오(1·3·5·7 만)도 1회
- [ ] `PATCH /api/clips/{id}/parts` — in/out 미세 조정. 기획서 Clip Studio 에도 있다
- [ ] 큐 위치 표시 — 워커가 하나라 대기가 생긴다
- [x] 라우트 배치 결정(D12) — 2026-09-16. `/` 첫 화면 · `/watch` 시청자 · `/studio` 스튜디오

---

## 4. 해커톤 이후 (제출 범위 밖)

**기획서에 있지만 8일 안에 못 하는 것들이다.** 문서에서 로드맵으로 옮기는 편이 낫다.

| 항목 | 왜 미루나 |
|---|---|
| 유튜브 OAuth 채널 연동 | 심사·검수 절차가 있고 본인 소유 채널이 전제다 |
| 라이브 채팅 실시간 수집 | **진행 중인 방송이 있어야 시험할 수 있다.** 지금 없다 |
| Type A 밀도 급증 탐지 | 채팅이 전제. 데모 시나리오의 밀도 그래프도 여기 종속 |
| Shorts 자동 업로드 | OAuth + 업로드 쿼터. 지금은 파일을 우리가 서빙한다 |
| Console(회차 추이·다중 채널) | 기획서도 MVP 제외. 회차가 쌓여야 볼 것이 생긴다 |
| pgvector | N<1000 이라 numpy 가 1ms 밑이다. 수만 개가 되면 켠다 |
| 자막 우선 전사(json3) · 챕터 씨앗 | 채널 사용 허락이 선행. 컬럼은 이미 있다 |
| 단어 경계 cut · 컨텍스트 캐싱 | 지금 병목이 아니다 |
| FILM(영화) 단계 | 2단계. 등록은 되고 첫 단계에서 막힌다 |
| Zoom · Slido CSV 경로 | 확장 신호원 |

---

## 5. 8일을 어디에 쓸까

제출 9/20. **라이브로 보여줄 것을 먼저 확보하고, 그 다음이 근거다.**

| 순서 | 할 일 | 크기 | 이유 |
|---|---|---|---|
| 1 | ~~두 번째 영상 등록·전사·분할~~ | 사람 시간 | 없으면 데모 6단계가 원리적으로 불가능. 전사에 수십 분 든다 |
| 2 | ~~미응답 리포트 1장~~ | 완료 | 리포트 탭 + 글로 복사 |
| 3 | 🔴 ~~리허설 도구~~ + **3회 리허설** | 도구 완료 | 답하기 1회가 7분 걸린 적이 있다. 리허설은 아직 0회 |
| 4 | ~~질문당 평균 비용~~ | 완료 | 실측 18원/질문 |
| 5 | CTA 목적지 결정 + 구현 | 작음 | PM 결정이 선행 |
| 6 | 답변 경로 평가 세트 | 중간 | 본선 발표 근거 |
| 7 | 질문 카테고리 | 중간 | Q-Rail 필터 + 리포트 분포 |
| 8 | 체크리스트 커버리지 | 중간 | 콜드스타트 해법. 시간이 남으면 |

**리스크 하나를 미리 적어 둔다.** Gemini 가 "high demand" 503 을 몇 분씩 낸다. 답하기 한 번이
**428초** 걸린 적이 있다(대부분 재시도). 재시도 예산을 62초로 늘렸지만 근본 해결은 아니다.
리허설에서 실측하고, 라이브 단계(시나리오 4·6)가 너무 길면 **미리 만들어 둔 것을 보여주는
경로**를 준비한다.

---

## 6. 하지 않기로 한 것

| 안 하는 것 | 이유 |
|---|---|
| LLM 관측 도구(Langfuse 등) 도입 | 비용·지연·토큰의 정본이 `stage_calls` 로 이미 있어 **정본이 둘**이 된다. 빠진 것은 본문 둘뿐이었고 컬럼 둘로 해결했다(마이그레이션 005). 프롬프트에 남의 콘텐츠 대사가 통째로 들어가는 것도 걸린다 |
| 기회손실 추정치 발표 | 전환 표본이 0이라 근거가 없다. 근거 없는 숫자는 심사에서 역효과다 |
| 예산 30초 상향 | 숏폼 형식의 전제다. 60초를 넘기면 제품 정의가 흔들린다 |
| 타인 영상 URL 지원 중단 | 기획서는 본인 소유 채널만 처리한다고 했지만, 지금 구현은 URL 로 받는다. **제출 전에 바꾸지 않는다** — 데모가 그 경로로 돌아간다. 문서에서 현황을 밝힌다 |
