// 이름 둘 — 화면에 보이는 것과 코드·레포의 것.
//
// 해커톤 발표명은 TEASE 다. 레포·파이썬 패키지·도커 서비스 이름은 shorts_maker 로 두고 바꾸지
// 않는다(update_plan D9: 개명은 위험만 있고 이득이 없다). 그래서 사람이 보는 제목은 PRODUCT_NAME,
// "서버 프로세스가 떠 있는지" 같은 운영 문장은 REPO_NAME 을 쓴다.
//
// 🔴 index.html 의 <title> 은 모듈을 import 할 수 없어 같은 값을 손으로 적어뒀다. 여기를 바꾸면
// 거기도 바꾼다.
export const PRODUCT_NAME = "TEASE";
export const REPO_NAME = "shorts_maker";
